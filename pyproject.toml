[project]
name = "consulta"
version = "0.1.0"
description = "Painel de Consulta"
requires-python = ">=3.7"
dependencies = [
    "requests",
    "TerminalButtons"
]

[project.scripts]
app = "main:run"
